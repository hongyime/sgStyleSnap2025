{
  "task": "Complete the private upload path for the accepted lossless Supabase migration",
  "baseline": "75d544c9b6e9de14fa92f248b6c7f5f0feec1d8a",
  "branch": "fix/retain-upload-drafts",
  "previous_pass": "PR139 presentation source and production verified, private delivery remains disabled",
  "stage": "Focused upload bug correction: local validation passes; hosted validation and production release pending. Private Supabase transport/SQL remain unimplemented.",
  "regressions": {
    "service_before_fix": "two failures: selected-image errors resolved with successful placeholder records",
    "forms_before_fix": "three failures: original_file missing in both forms and dialog submitted during background processing",
    "after_fix": "six focused tests pass; originals and processed files retained for retry"
  },
  "catalog_decision": "Live auto-contribution publishes all upload images anonymously, regardless of the clothing privacy setting. Preserve retained records. Ask in the same HTML whether to preserve that behavior or require public/explicit opt-in for future catalog contribution.",
  "implementation_direction": [
    "Use a Supabase Edge endpoint with server-verified user identity; service privileges stay inside the endpoint",
    "Bound upload bodies before parsing and calculate hashes from received original and processed bytes, not client assertions",
    "Use immutable content-addressed objects with no upsert, trusted read-back and durable reservations for uncertain uploads",
    "Use owner/request IDs for replay and return stored receipts after lost responses",
    "Extend binding version identity without deleting old mappings; the initial publication completion gate must count original plan members while allowing verified later upload versions",
    "Retain pre-update source rows in the private operation journal",
    "Keep original-byte/provenance access separate from processed-image sharing",
    "Include catalog contribution/adoption and later avatars in the complete writer audit before activation"
  ],
  "findings": [
    "Selected-file failures can currently become successful placeholder records",
    "Both upload forms replace the selected original with a processed file",
    "Existing binding primary keys cannot retain multiple image versions for one field in the active manifest",
    "The live clothes table has automatic catalog contribution, category and 50-item quota triggers; their effects must be included in publication tests"
  ],
  "required_design": [
    "Preserve original and processed bytes immutably",
    "Authenticate server-side and derive ownership from verified identity",
    "Keep clients unable to manufacture trusted hashes or bypass capacity",
    "Reserve both stored and uncertain bytes before network writes; never silently discard failed uploads",
    "Retain previous source rows and bindings when replacing an image",
    "Publish the clothing row, any legitimate catalog contribution and delivery bindings atomically",
    "Bound requests, file bytes and retry attempts",
    "Keep activation gated by complete migration parity, hosted privacy, current quota and rollback"
  ],
  "complete": false,
  "regression_results": {
    "service_and_forms": 11,
    "all_unit": 105,
    "type_check": "pass",
    "build": "pass",
    "new_reproductions": [
      "database update removed selected files and changed prior URLs before success",
      "recognition-error previews accumulated until unmount"
    ]
  }
}
