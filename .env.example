# StyleSnap - Environment Variables Template
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control

# ==================================================
# SUPABASE (REQUIRED)
# ==================================================
# Get from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key-here
# Leave false until archive/delivery parity, uploads, hosted permissions,
# source delta, rollback and measured Free capacity have all passed.
# A missing or denied binding displays a local placeholder when true.
VITE_PRIVATE_MEDIA_ENABLED=false

# ==================================================
# GOOGLE OAUTH (REQUIRED - Only Authentication Method)
# ==================================================
# Get from: https://console.cloud.google.com/
# 1. Create OAuth 2.0 Client ID
# 2. Add authorized redirect URI: https://your-project.supabase.co/auth/v1/callback
# 3. Add authorized redirect URI for dev: http://localhost:3000
# 4. Configure in Supabase Dashboard → Authentication → Providers → Google
# CRITICAL: Google SSO is the ONLY authentication method - no email/password
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# ==================================================
# CLOUDINARY (REQUIRED)
# ==================================================
# Get from: https://cloudinary.com/console
# 1. Copy Cloud Name from dashboard
# 2. Go to Settings → Upload → Add Upload Preset
# 3. Create UNSIGNED preset (no authentication required)
# 4. Set folder (optional): stylesnap-uploads
VITE_CLOUDINARY_CLOUD_NAME=your-cloud-name
VITE_CLOUDINARY_UPLOAD_PRESET=your-unsigned-upload-preset

# ==================================================
# PUSH NOTIFICATIONS (REQUIRED for push features)
# ==================================================
# Generate VAPID keys:
# 1. Install web-push: npm install -g web-push
# 2. Generate keys: web-push generate-vapid-keys
# 3. Use public key here, store private key in Supabase Edge Function secrets
# 4. Set subject to mailto:your-email@example.com or https://your-domain.com
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key-here

# ==================================================
# OPTIONAL FEATURES
# ==================================================
# Weather API (optional - for weather-based outfit suggestions)
# Get from: https://openweathermap.org/api
VITE_OPENWEATHER_API_KEY=your-openweather-api-key-here

# ==================================================
# DEVELOPMENT SETTINGS
# ==================================================
# Set to 'production' for production builds
NODE_ENV=development

# Prepared catalog writer. Keep false until complete media migration and hosted checks pass.
VITE_PRIVATE_CATALOG_ADOPTIONS_ENABLED=false
